<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MAUI_Music.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MAUI_Music"
    xmlns:pages="clr-namespace:MAUI_Music.Pages"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>

        <Tab 
            Icon="play"
            Title="Écouter">    
            <ShellContent
            ContentTemplate="{DataTemplate pages:BibliothequePage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="grid"
            Title="Explorer">
            <ShellContent
            ContentTemplate="{DataTemplate pages:BibliothequePage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="radio"
            Title="Radio">
            <ShellContent
            ContentTemplate="{DataTemplate pages:BibliothequePage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="music"
            Title="Bibliothèque">
            <ShellContent
            ContentTemplate="{DataTemplate pages:BibliothequePage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="search"
            Title="Rechercher">
            <ShellContent
            ContentTemplate="{DataTemplate pages:BibliothequePage}"
            Route="MainPage" />
        </Tab>

    </TabBar>

</Shell>

