<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MAUI_Music.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MAUI_Music"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>

        <Tab 
            Icon="play"
            Title="Ecouter">
            <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="grid"
            Title="Explorer">
            <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="radio"
            Title="Radio">
            <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="music"
            Title="Bibliothèque">
            <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        </Tab>

        <Tab 
            Icon="search"
            Title="Rechercher">
            <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        </Tab>

    </TabBar>

</Shell>

